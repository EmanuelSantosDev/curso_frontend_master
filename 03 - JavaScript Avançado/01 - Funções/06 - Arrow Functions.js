// de linha única
const mutiplicacao = (n1, n2) => n1 * n2

console.log(mutiplicacao(6, 2)) // 12

// com múltiplas linhas
const subtracao = (n1, n2) => {
    resultado = n1 - n2
    return resultado
}

console.log(subtracao(6, 2)) // 4