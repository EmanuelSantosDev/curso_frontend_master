idade = 17

// armazenando o resultado em uma variável
maiorDeIdade = idade >= 18 ? 'SIM' : 'NÃO'
console.log(maiorDeIdade) // NÃO

// execução de comando via ternário
idade > 18 ? console.log('SIM') : console.log('NÃO') // NÃO