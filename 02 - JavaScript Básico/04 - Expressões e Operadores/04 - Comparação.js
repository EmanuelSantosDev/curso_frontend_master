let a = 10
let b = 5

// Igual (==)
let isEqual = a == b

// Diferente (!=)
let isNotEqual = a != b

// Maior que (>)
let isGreater = a > b

// Maior ou igual que (>=)
let isGreaterOrEqual = a >= b

// Menor que (<)
let isLess = a < b

// Menor ou igual que (<=)
let isLessOrEqual = a <= b 
